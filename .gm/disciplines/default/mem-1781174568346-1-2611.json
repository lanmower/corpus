## Corpus app.js god-module split IN PROGRESS (2026-06-11, commits 807231b->2e8ab5f)

User authorized full rearchitecture ('split it, rearchitecture everything to make maximum sense'). Prior sessions deferred citing shared module-private state + mutable stage. RESOLVED that coupling by extracting an explicit SPINE. Architecture: app-context.js (substrate: state, el/icon/iconLabel, dom refs, getStage/setStage, data-loading fetchJson/loadManifest/loadShard/loadAllShards, shared helpers loadGuideTicks/masteryFor/sectionCardCounts/exportSessionCards/dueCountFor/totalDueAll/dueCountsBySubject/totalCasesQueued/casesDoneBySubject/estReviewMinutes/todayPlanReviewTarget/channel/emit/updateFooter; zero upward imports) <- router.js (ROUTES/ALIASES/go/render via setRenderer injection + onNav hook; no static view import) <- views/*.js (import context+router) <- thin app.js (registers render+views, boot).

DONE+witnessed+committed: app-context (807231b), router (d3f8813), stage->getStage/setStage 64 sites (641ea41), views/stats.js (dce259d), views/calendar.js (f08ec80), views/settings.js + shortcuts.js (3eb4ac2), views/cases.js (2e8ab5f). app.js 2779->~2150 lines. test.js source-grep assertions repointed to new module locations as each extracted (READ('site/<mod>') instead of app.js); 24 pass throughout. Each view browser-witnessed (route renders + module exports + zero console errors). .turn-browser-witness via playwriter; SDK proxy-swap path uses setStage/getStage.

REMAINING PRD rows (pending, EXECUTE): arch-view-today, arch-view-review (KEYSTONE, ~560 lines, ~25-import surface incl tutor/undo/flag/confidence/late/newcards/cram + shared helpers slugify+totalNewEligibleAll+buildFlashcard+FRIENDLY_GRADES; blocks mistakes/drill/today which call resetReviewQueue/gradeReview), arch-view-guides, arch-view-subject, arch-view-mistakes-drill, arch-search-palette, arch-app-thin, arch-test-migration(full), arch-witness-all-routes.

EXTRACTION PATTERN that works: sed line-range extract block -> prepend imports + 'export ' on the route fn -> write views/x.js -> node splice the lines out of app.js -> add import to app.js -> node test.js (repoint any failing READ('site/app.js') grep to the new module) -> browser witness route renders -> git_finalize commit. buildFlashcard is shared review+guide (extract to views/flashcard.js leaf next). keyboard handler in app.js (1531-1583) stays but imports gradeReview/undoLastGrade/skipReview/renderReview/FRIENDLY_GRADES from review module. slugify+totalNewEligibleAll location: verify before review extraction (likely app.js guide-builder + today helper).
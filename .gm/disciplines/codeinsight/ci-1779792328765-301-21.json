./scripts/build_data.js:301:309 tokenize
function tokenize(s) {
    const out = [];
    for (const w of String(s || '').toLowerCase().split(/\W+/)) {
        if (w.length < 3) continue;
        if (STOPWORDS.has(w)) continue;
        out.push(w);
    }
    return out;
}
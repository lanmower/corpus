## Corpus app.js god-module split PROGRESS UPDATE (2026-06-11, through commit 58f5902)

Updates mem about commits 807231b->2e8ab5f. NOW DONE+witnessed+committed+pushed (12 commits total this session): app-context.js spine (807231b), router.js (d3f8813), stage->getStage/setStage (641ea41), views/stats.js (dce259d), views/calendar.js (f08ec80), views/settings.js+shortcuts.js (3eb4ac2), views/cases.js (2e8ab5f), removed dead buildFlashcard (98acf7f), moved slugify+totalNewEligibleAll to app-context (1a1fe24), views/review.js KEYSTONE (947d8cd), views/mistakes.js+views/drill.js (58f5902). app.js 2779->~1480 lines. test.js 24 pass throughout; each route browser-witnessed renders + zero console errors.

The review keystone (the coupling prior sessions said blocked the split) IS DONE: views/review.js owns renderReview+resetReviewQueue+gradeReview+undoLastGrade+skipReview+suspendCurrentReview+collectReviewTags+FRIENDLY_GRADES, ~13 imports. app.js imports the 6 public verbs for keyboard+today/subject/mistakes/drill+onNav+fns-map. mistakes/drill now import resetReviewQueue from review.js.

REMAINING PRD rows (pending): arch-view-today, arch-view-guides, arch-view-subject, arch-search-palette, arch-app-thin, arch-test-migration(full), arch-witness-all-routes.

TODAY CLUSTER is fragmented (interleaved with renderSubject): renderCramBanner(100)/renderResumeLine(120)/isFirstTimeVisitor(134)/renderWelcome(148)/renderTutorOverviewPanel(164)/renderTodayPrimary(232)/renderNextReadingCard(295)/renderToday(342) form range 100-489; then renderSubject(490-1078) + renderExamDay(1079, KEEP in app.js, called by render()); then today-helpers renderSparkline(1090)/renderMasteryRing(1119)/renderScheduleChecklist(1159). renderStatusLine(87) is DEAD (1 ref, remove). So today extraction = two splices (100-489 and the 3 helpers ~1090-1209) into views/today.js. SUBJECT/GUIDES = renderGuides + renderSubject(~590 lines) + buildGuideToc/buildChunkedGuide/buildVideoHero/buildAudioPanel/buildInfographicsPanel + infographic lightbox; uses slugify(context), markdown, buildFlashcard is GONE. app-thin = setupSdkApp (large SDK bootstrap, stays) + mountSearchPalette + keyboard handler + boot; app.js ends as orchestrator ~600-700 lines (acceptable).

EXTRACTION PATTERN (works): node-splice line ranges (account for CRLF: at(n).replace(/$/,'')), prepend imports+export on route fns, write views/x.js, splice out of app.js, add import, node test.js (repoint failing READ('site/app.js') greps to new module, e.g. combine app+module sources for cross-file counts), browser-witness via hash-nav (NOT programmatic renderX() which destroys eval context), git_finalize. WATCH: include getStage/el in import list for any fn that renders a section-head (drill.js missed it -> 'getStage is not defined' caught by witness).
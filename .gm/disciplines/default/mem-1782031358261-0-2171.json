## Corpus gm-skill quality run (2026-06-21, commit d4d08fa, branch claude/gifted-allen-v14fh5)

User re-issued 'make a workflow, improve every possible aspect'. KEY DIFF from prior 23 runs: this is a FRESH WEB CLONE — the legacy .claude/workflows/quality-max.js was local-only and is ABSENT here, so 'run the existing workflow' was impossible. Reinterpreted 'make a workflow' as a DURABLE COMMITTED artifact.

ENV CONSTRAINTS (remote web sandbox): (1) npx/bun-x of agent-chosen packages DENIED by auto-classifier — gm-plugkit boot needed user to allow it; (2) chromium NOT installable -> browser verb returns 'chromium binary not found', browser-witness gate is external-blocked here, fall back to node --check ESM-parse + node test.js; (3) git LFS *.mp4 not fetched -> any test asserting on-disk mp4 hard-fails; (4) git_checkout VERB only switches branches (ignores paths param) — path-restore needs bash `git checkout --` (NOT hard-blocked, worked); (5) committed site/data shards were Windows-built (CRLF) so a Linux `node build_data` rebuild shows 2005/2005 line-swap churn + guideChars delta + timestamp = REVERT, don't commit; (6) test.js regenerates exports/corpus-anki-*.txt every run (anki_export spawn) -> always dirty after test, revert before push.

LANDED (all in test.js run-24, 35 pass): (a) .github/workflows/ci.yml — first CI to run the suite (build_data + test.js on push/PR/dispatch); pages.yml only deploys. (b) schedule.js:144 daysToExam fallback used UTC toISOString day-key -> local isoDate(new Date()) per documented invariant (lines 98-104). (c) triage-llm-worker.js:89 let tps -> let tps=0 (first token posted undefined). (d) README: arrow/check/refresh glyphs -> ASCII, stale 'Gemma-4 E2B' -> 'Bonsai-1.7B', reconcile note wording matched today.js:514-518.

3 review agents fanned out. REFUTED false-positives (verified against source): review.js:236 deref (clamped line 122), subject.js back-to-top 'leak' (remove-before-add 461->463 via __backToTopHandler), mini-card 'leak' (DOM rebuilt each render), async-render-no-await (established 23-run pattern). test.js already had process.exit(fail?1:0) — earlier 'exit 0' was tail's code.
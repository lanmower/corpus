## Corpus quality-max 3rd run (2026-06-10 session 3, commit 7d2e92b)

User re-issued 'make a workflow to maximize quality, fan out subagents, engineering-DNA discipline'. The workflow ALREADY EXISTED (.claude/workflows/quality-max.js, commits 2c3bede/b05282e) and is a registered skill. Correct move = RUN it, not rebuild (Principle 2/3). wf_6490f4c6-884: 16 agents, 8 raw -> 7 confirmed / 1 refuted.

7 CONFIRMED + FIXED:
1. app.js daily check-in dedup used new Date().toISOString() (UTC) while gate shouldCheckInToday() uses local todayStamp() -> sub-UTC users miss check-in at local midnight. Fix: exported tutor-store todayStamp as localDateStamp, import it. Schedule-block filter (line 403) stays UTC to match schedule.js isoDate.
2. srs.js compressInterval: NaN effectiveDays (corrupt examDate) slips `<=0` -> NaN interval/dueAt -> NaN>now always false -> card permanently due. Fix: Number.isFinite guard + loadConfig examDate validation.
3. build_data.js loadGuide emitted sections w/o body -> tutor.js grounding on empty strings. Fix: attach prose body between headings. Now 6/8 cardiology sections have body.
4. theme.js auto only listened prefers-color-scheme, not prefers-contrast -> high-contrast toggle needs reload. Fix: loop both queries.
5. triage-live.js no thinking watchdog (tutor-panel has armThinkingWatchdog) -> WebGPU device-lost leaves composer disabled forever. Fix: setGenerating() owns flag+90s watchdog, replaced 4 direct assignments.
6. srs.js migrate() passed future schema versions through -> saveStates relabels as v1. Fix: throw on unknown version; importState caller already alerts.
7. serve.js dead setInterval(()=>{},60000) removed (http.Server keeps loop alive).

GOTCHAS: browser verb body is RAW JS (page/page.evaluate globals), NOT a {url,script} JSON envelope. Use console.log(JSON.stringify(out)) to capture witness in stdout (return value not captured). long-gap-no-instruction gate fires at 300s idle mid-EXECUTE - dispatch instruction before browser. test.js scans theme.js for ASCII-cleanliness - em-dash in comments fails it.
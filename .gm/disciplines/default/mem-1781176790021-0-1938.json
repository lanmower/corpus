## Corpus app.js god-module split COMPLETE (2026-06-11, HEAD 61f15d0)

FULLY DONE: all 10 route views extracted onto an explicit composition spine. app.js 2779 -> 443 lines (an orchestrator: imports + render() dispatch map + setRenderer + onNav hook + renderExamDay + openQuickAdd + showStorageFullBanner + keyboard handler + mountSearchPalette + setupSdkApp SDK bootstrap + boot). Architecture: app-context.js (substrate: state/el/icon/dom/getStage-setStage/data-loading/shared-helpers incl slugify+totalNewEligibleAll; zero upward imports) <- router.js (ROUTES/go/render via setRenderer injection + onNav hook; view-registry-free) <- views/{today,guides,subject,review,cases,stats,calendar,settings,mistakes,drill}.js + shortcuts.js leaf. 16 commits this session 807231b..61f15d0. test.js 24 pass throughout (every source-grep assertion repointed to its new module; cross-file counts summed; dead-code assertions for renderStatusLine/buildFlashcard/copy-id dropped). Every route browser-witnessed renders + zero console errors; final all-routes sweep browser-31 clean.

KEY RESOLUTIONS: the shared module-private state + mutable stage that blocked prior split attempts -> state owned by app-context, stage behind getStage/setStage (SDK proxy-swap routes through setStage). resetReviewQueue/gradeReview shared by today/mistakes/drill/keyboard -> exported from views/review.js. renderCramBanner shared today+subject -> exported from today.js, subject imports it. slugify/totalNewEligibleAll -> app-context. Dead code removed: buildFlashcard (0 callers), renderStatusLine (0 callers).

KNOWN FOLLOW-UP (harmless, not blocking): app.js retains ~40 now-unused leaf-lib imports (cram/justread/drill/mistakes/etc whose renderers moved); no bundler so zero runtime cost; a clean auto-detector was unreliable (deep 'from ./' in body confuses the import/body split) so left for a careful manual subtractive pass. Pattern for continuation in mem3.
## Corpus quality-max 4th run (2026-06-10 session 4, commit 6032078)

User re-issued 'make a workflow to maximize quality, fan out subagents, engineering-DNA discipline, consider everything not yet considered'. Workflow ALREADY EXISTED (.claude/workflows/quality-max.js, commits 2c3bede/b05282e/7d2e92b). Correct move = RUN it, not rebuild (Principle 2/3). wf_6f8607b0-88c: 14 agents, 6 raw -> 6 confirmed / 0 refuted. This run hit subsystems prior 3 runs skipped: review section-ref navigation, session-done export, srs config write, clipboard degradation.

6 CONFIRMED + FIXED (5 code, 1 accepted):
1. MEDIUM app.js:1637/1653 section-ref number-vs-string ===: shard.guide.sections[].line is NUMBER (build_data 'line: heads[h].line', the heading index i), card.requires.sectionLine is STRING ('sectionLine: String(best.line)'). Bare s.line===card.requires.sectionLine ALWAYS FALSE -> section-reference link always fell back to generic 'guide section' title AND post-nav setTimeout scroll-to-section never resolved anchor (page never scrolled/expanded to linked section). Fixed both builder + click handler to String()-coerce both sides (every other call site already did, e.g. app.js:780). browser-witnessed on triage page: oldMatch=false newMatch=true, 0 console errors, 18 nav links.
2. MEDIUM app.js:1598 export-session-cards always []: button filtered state.reviewQueue but gradeReview splices each id out of reviewQueueIds (1844) so queue empty by done-time -> exportSessionCards always hit empty-guard 'No cards to export'. Data-first fix (Principle 1): retain graded card in new state.reviewSessionCards (push card0 in gradeReview, reset in resetReviewQueue + on review-more chip), export reviewSessionCards.slice().
3. LOW srs.js:205 saveConfig no merge (adversarial): wrote JSON.stringify(cfg) verbatim, unlike tutor-store.saveConfig which merges. Partial call saveConfig({sessionGoal}) silently wipes examDate — undocumented caller obligation (app.js:2239 manually re-spreads). Fixed: const merged={...loadConfig(),...cfg}. No importState regression (exportState writes full config).
4+5. LOW app.js clipboard x2 (worst-case): copy-id (1364) navigator.clipboard.writeText unguarded -> sync TypeError in insecure context (file://, plain HTTP), no feedback. exportSessionCards (167) .catch never catches the SYNCHRONOUS throw when navigator.clipboard undefined. Fixed: shared copyToClipboard + fallbackCopy (execCommand) + always-on toast (mirrors tutor-panel copyText).

ACCEPTED-as-is: WORKER_CONTEXT_TURNS=16 dup tutor.js:20 / tutor-store.js:15 (composition-spine). tutor.js is a CDN-only module worker (imports ONLY transformers.js from jsdelivr, zero local imports); importing tutor-store.js pulls localStorage/window-referencing module code into the worker boundary. Existing test guardrail (test.js:1074-1076 drift assert) already prevents the desync class. Principle 2/7 — guardrail mitigates, import adds risk.

test.js: 20 pass (was 19) — added run-4 guardrail block asserting String-coercion x2, reviewSessionCards retention, saveConfig merge, copyToClipboard+fallbackCopy, no unguarded writeText.

KEY: 4th consecutive run still found a MEDIUM real bug (section-ref nav dead) in a subsystem 3 prior runs never reviewed — the selection-pressure loop keeps paying out. browser verb networkidle TIMES OUT on corpus (SDK/worker keep connections); use waitUntil:'domcontentloaded' + waitForTimeout.